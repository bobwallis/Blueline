imports:
    - { resource: parameters.ini }
    - { resource: security.yml }

framework:
    #esi:             ~
    #translator:      { fallback: en }
    secret:          %secret%
    charset:         UTF-8
    router:          { resource: "%kernel.root_dir%/config/routing.yml" }
    form:            true
    csrf_protection: true
    validation:      { enable_annotations: true }
    templating:      { engines: ['twig'] } #assets_version: SomeVersionScheme
    session:
        default_locale: %locale%
        auto_start:     false

# Twig Configuration
twig:
    debug:            %kernel.debug%
    strict_variables: %kernel.debug%
    globals:
        admin_email: %admin_email%
        database_update: %database_update%

# Twig Extensions
services:
    blueline.twig.extension:
        class: Blueline\Extensions\Twig\All
        tags:
            - { name: twig.extension }

# Assetic Configuration
assetic:
    debug:          %kernel.debug%
    filters:
        cssrewrite: ~
        less:
            node: /usr/bin/node
            node_paths: [/usr/local/lib/node_modules]
        closure:
            jar: %kernel.root_dir%/Resources/java/compiler.jar
        yui_css:
            jar: %kernel.root_dir%/Resources/java/yuicompressor.jar

# Doctrine Configuration
doctrine:
    dbal:
        driver:   %database_driver%
        host:     %database_host%
        port:     %database_port%
        dbname:   %database_name%
        user:     %database_user%
        password: %database_password%
        charset:  UTF8
    orm:
        auto_generate_proxy_classes: %kernel.debug%
        entity_managers:
            default:
                mappings:
                    BluelineCCCBRDataBundle: ~
                dql:
                    string_functions:
                        regexp:
                            Blueline\Extensions\Doctrine\Regexp
                        concat_ws:
                            Blueline\Extensions\Doctrine\ConcatWs
                        levenshtein_ratio:
                            Blueline\Extensions\Doctrine\LevenshteinRatio
                    numeric_functions:
                        acos:
                            Blueline\Extensions\Doctrine\Acos
                        cos:
                            Blueline\Extensions\Doctrine\Cos
                        sin:
                            Blueline\Extensions\Doctrine\Sin
                        radians:
                            Blueline\Extensions\Doctrine\Radians

# Swiftmailer Configuration
swiftmailer:
    transport: %mailer_transport%
    host:      %mailer_host%
    username:  %mailer_user%
    password:  %mailer_password%

jms_security_extra:
    secure_controllers:  true
    secure_all_services: false
